/**
 * Service Package
 * ===============
 * This package contains service classes.
 * Services hold the "business logic" — the brain of our app.
 *
 * For example, when a user places an order, the service:
 *   1. Checks if the cart has items.
 *   2. Calculates the total price.
 *   3. Saves the order to the database.
 *   4. Clears the cart.
 *
 * Controllers call services. Services call repositories.
 * Controller → Service → Repository → Database
 */
package com.ecommerce.service;
