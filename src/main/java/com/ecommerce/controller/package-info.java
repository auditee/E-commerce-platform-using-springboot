/**
 * Controller Package
 * ==================
 * This package contains REST API controllers.
 * Controllers are the "front door" of our backend.
 * They receive HTTP requests from the frontend (or Postman)
 * and return HTTP responses.
 *
 * Example: ProductController will handle requests like:
 *   GET  /api/products     → return all products
 *   POST /api/products     → create a new product
 */
package com.ecommerce.controller;
