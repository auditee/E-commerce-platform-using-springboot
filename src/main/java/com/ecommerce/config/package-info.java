/**
 * Config Package
 * ==============
 * This package contains configuration classes.
 * Configuration classes tell Spring Boot HOW to set things up.
 *
 * For example:
 *   - Which URLs require login and which are public.
 *   - How to encode (hash) passwords so they are stored securely.
 *   - CORS settings (allowing the React frontend to talk to this backend).
 */
package com.ecommerce.config;
