/**
 * Entity Package
 * ==============
 * This package contains entity classes.
 * Each entity class represents one table in the MySQL database.
 *
 * For example:
 *   User.java    → "users" table
 *   Product.java → "products" table
 *   Order.java   → "orders" table
 *
 * Hibernate (part of JPA) reads these classes and automatically
 * creates or updates the corresponding database tables.
 *
 * Each field in the entity = one column in the table.
 */
package com.ecommerce.entity;
